# 9Router v0.5.35 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce, verify, deploy, and document customized 9Router `0.5.35` while preserving every required local invariant and updating public pull requests.

**Architecture:** Merge the complete customized `0.5.30` branch into a clean `v0.5.35` worktree. Resolve only release-overlap conflicts, test source before building, validate an isolated copied-DB candidate, then atomically promote once.

**Tech Stack:** Node.js, Next.js standalone build, Vitest, SQLite, PM2, cloudflared, Bash, Git, GitHub CLI.

## Global Constraints

- Live data path is `/home/home/.9router`.
- Live app path is `/home/home/.openclaw/workspace-keyra/9router-patch/cli/app`.
- Live port is `20128`; candidate port is `20129` on `127.0.0.1` only.
- Short URL is `https://rkeyra9.abc-tunnel.us`.
- Preserve `18888` as DC and `18889` as residential; do not merge roles.
- Preserve Stripe filtering and all gateway tags/domain filters.
- Preserve private Grok strict residential pool and private model aliases in DB only.
- Preserve best-GPT target `cx/gpt-5.6-sol`, effort `max`, and service tier `default`.
- No live restart before source, build, copied-DB candidate, and rollback checks pass.

---

### Task 1: Merge And Reconcile Source

**Files:**
- Modify: ten conflict files reported by `git merge-tree`
- Add: existing local ledger, runbook, verifier, codec, tests, and support modules from `local-v0.5.30-upgrade`

- [ ] Merge `local-v0.5.30-upgrade` with `git merge --no-ff --no-commit local-v0.5.30-upgrade`.
- [ ] Resolve conflicts using upstream `0.5.35` GitHub native Claude and Grok subscription code as base.
- [ ] Retain P1-P20 actions from design matrix; remove no-op duplicate P5M/P10 code.
- [ ] Run `git diff --check` and reject every conflict marker.
- [ ] Run local verifier against source and live DB; bundle checks remain pointed at current live until new build exists.
- [ ] Commit reconciled source as one audited merge.

### Task 2: Source QA

**Files:**
- Test: focused files named in `docs/PATCH_LEDGER.md`
- Test: all changed files from `v0.5.35..HEAD`

- [ ] Install locked dependencies without changing manifests.
- [ ] Run syntax and focused ESLint checks.
- [ ] Run all focused P1-P20 Vitest files.
- [ ] Run full Vitest suite and compare failures with clean `v0.5.35` baseline.
- [ ] Run exact estimator equivalence and Grok pinned-source checks.
- [ ] Fix changed-path failures with red tests first, then rerun focused and full checks.
- [ ] Commit source QA fixes separately.

### Task 3: Build And Bundle Verification

**Files:**
- Modify: `/home/home/.openclaw/workspace-keyra/9router-ops/safe-promote-app.sh`
- Generate: staged standalone candidate app outside live path

- [ ] Update promotion helper to accept source path and expected version, defaulting to `0.5.35` for this release.
- [ ] Run helper shell syntax check and a pre-swap dry validation against an invalid version fixture.
- [ ] Build with `NINEROUTER_CLI_APP_DIR` pointing to staged destination.
- [ ] Verify package version, custom server entrypoint, MITM bundle, routes, and all bundle markers.
- [ ] Run source/bundle/live-DB verifier and `sqlite3 ... 'pragma integrity_check'`.

### Task 4: Isolated Candidate QA

**Files:**
- Generate: copied candidate home and DB backup
- Delete after QA: credential-bearing candidate home and payloads

- [ ] Copy DB with SQLite `.backup`; remove every refresh token from copied DB.
- [ ] Start candidate on `127.0.0.1:20129` with no tunnel and copied home.
- [ ] Run health, version, models, OAuth proxy, usage, console, quota, and API-client checks.
- [ ] Run GPT-5.6 Sol/max, Responses Lite, GitHub Claude Responses, and Grok codec canaries.
- [ ] Run deterministic 400 no-lock check, concurrent health probes, and approximately 1 MB/463-item Grok replay.
- [ ] Stop candidate; verify copied DB integrity; delete copied home and temporary payloads.

### Task 5: Update Public Pull Requests

**Files:**
- Modify only clean public PR worktrees; private ledger, aliases, pools, DB, and deploy files excluded.

- [ ] Re-inventory open PRs with `gh pr list --repo decolua/9router --author ryanngit`.
- [ ] For each PR, classify merged, obsolete, clean rebase, conflict repair, or replacement.
- [ ] Update still-relevant branches onto `v0.5.35`; preserve existing PR numbers when practical.
- [ ] Run branch-focused tests, ESLint, `git diff --check`, and private-data scans.
- [ ] Push with normal fast-forward where possible; use `--force-with-lease` only for a verified rebased branch.
- [ ] Confirm GitHub PR state and record new heads in ledger.

### Task 6: Atomic Promotion And Retrospective

**Files:**
- Modify: `docs/PATCH_LEDGER.md`
- Modify: `docs/UPDATE_RUNBOOK.md`
- Modify: `scripts/verify-local-patches.mjs` only when `0.5.35` changes require new markers

- [ ] Record PM2, tunnel PID, raw URL, short URL, active traffic, package versions, and DB integrity.
- [ ] Run detached promotion helper through two quiet snapshots and one PM2 restart.
- [ ] Verify local, raw, and short health; version agreement; PM2 custom-server entrypoint; tunnel recovery.
- [ ] Run live GPT, Lite, GitHub Claude, Grok, usage, console, quota, and API-client canaries.
- [ ] Run final source/live/DB verifier and DB integrity checks.
- [ ] Update ledger/runbook with classification, conflicts, tests, PR heads, backup, PIDs, URL, downtime, and rollback status.
- [ ] Commit final evidence and leave rollback app intact.
